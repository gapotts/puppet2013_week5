# Exercise 2 solution, create roles:
#                           mysql
#                           webserver
#                           webserver/dev
#                           base
#
# Apply with:
sudo puppet apply --verbose --modulepath=./modules -e 'include roles::webserver'
