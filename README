# Exercise 2, create roles:
#                           mysql
#                           webserver
#                           lamp-dev
#                           base
#
# Apply with:
sudo puppet apply --verbose --modulepath=./modules -e 'include profiles::apache'
